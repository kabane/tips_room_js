%p#notice= notice
%h1 Tips
%table
  %thead
    %tr
      %th Title
      %th Contents
      %th{:colspan => "3"}
  %tbody
    - @tips.each do |tip|
      %tr
        %td= tip.title
        %td= tip.contents
        %td= link_to 'Show', admin_tip_path(tip)
        %td= "下書き" if tip.draft?
%br/
= link_to 'New Tip', new_admin_tip_path if current_user.present? && current_user.try(:admin?)
